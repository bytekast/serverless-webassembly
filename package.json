{
  "scripts": {
    "postinstall": "wasm-pack build --target nodejs",
    "watch": "watch 'npm i' ./src",
    "local": "sls offline"
  },
  "devDependencies": {
    "serverless-offline": "^12.0.4",
    "watch": "^1.0.2"
  },
  "name": "my-service"
}
